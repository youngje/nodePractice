// A few variables, hanging around
var cow, x;
cow = "sleepy";

// A simple function that doubles its argument
var double = function (a) {
    cow = "purple";
    return a * 2;
};

x = double(6);
// What is cow now?